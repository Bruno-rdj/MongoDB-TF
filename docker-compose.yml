version: '3'
services:
  mongo:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: Bruno
      MONGO_INITDB_ROOT_PASSWORD: 2503

volumes:
  mongo_data:
    driver: local
